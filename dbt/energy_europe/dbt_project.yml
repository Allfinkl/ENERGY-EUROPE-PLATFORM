name: 'energy_europe'
version: '1.0.0'
config-version: 2

profile: 'energy_europe'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  energy_europe:
    staging:
      +materialized: view
    marts:
      +materialized: view
